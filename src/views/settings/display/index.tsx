import { ContentSection } from '../components/content-section';

export function SettingsDisplay(): React.JSX.Element {
  return (
    <ContentSection title="Display" desc="Turn items on or off to control what's displayed in the app.">
      <p className="text-sm text-muted-foreground">Display settings coming soon.</p>
    </ContentSection>
  );
}
